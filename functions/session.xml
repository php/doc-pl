<?xml encoding="iso-8859-2"?>
<!-- $Revision: 1.2 $ -->
 <reference id="ref.session">
  <title>Funkje obsługi sesji</title>
  <titleabbrev>Sesje</titleabbrev>
	
  <partintro>
   <para>
    Obsługa sesji w PHP ma na celu zapewnienie sposobu na zachowanie pewnych
    danych w trakcie następujących po sobie wywołań strony. Pozwala to na
    budowanie bardziej spersonalizowanych aplikacji i zwiększenie
    atrakcyjności twojej strony internetowej.
   </para>
   <para>
    Jeśli jesteś zaznajomiony z zarządzaniem sesją w PHPLIB, zauważysz że
    pewnie koncepcje są podobne w obłudze sesji PHP.
   </para>
   <para>
    Gość wchodzący na twoją stronę WWW otrzymuje unikalny identyfikator, tzw.
    id sesji. Jest ono przechowywane albo jako ciasteczko po stronie
    użytkownika lub propagowane w URL'u.
   </para>
   <para>
    Obsługa sesji pozwala ci na rejestrowanie dowolnej ilości zmiennych, które
    mają być przekazywane pomiędzy stronami. Kiedy gość wchodzi na twoją
    strone, PHP automatycznie sprawdzi (jeśli session.auto_start jest ustawione
    na 1) lub na twoje życzenie (jawnie przez wywołanie
    <function>session_start</function> lub niejawnie przez wywołanie
    <function>session_register</function>) czy specyficzne id sesji zostało
    przypisane. Jeśli tak, poprzednio zachowane środowisko jest odtwarzane.
   </para>
   <para>
    Wszystkie zarejestrowane zmienne są serializowane po wykonaniu całego kodu
    strony. Zarejestrowane zmienne, które są niezdefiniowane, są zaznaczane
    jako niezdefijiowane. Nie są one definiowane przez moduł sesji w
    następujących po sobie wywołaniach, chyba że użytkownik zdefiniuje je
    później.
   </para>
   <para>
    Opcje konfiguracyjne <link
    linkend="ini.track-vars"><literal>track_vars</literal></link> i
    <link
    linkend="ini.register-globals"><literal>register_globals</literal></link>
    wpływają na to, jak zmienne sesyjne są przechowywane i odtwarzane.
   </para>

   <note>
    <para>
     Od PHP w wersji 4.0.3 opcja <link
     linkend="ini.track-vars"><literal>track_vars</literal></link> jest zawsze
     włączona.
    </para>
   </note>

   <para>
    Jeśli włączona jest opcja <link
    linkend="ini.track-vars"><literal>track_vars</literal></link> a
    <link
    linkend="ini.register-globals"><literal>register_globals</literal></link>
    jest wyłączona, tylko pozycje należące do zmiennej asocjacyjnej
    $HTTP_SESSION_VARS mogą być zarejestrowane jako zmienne sesyjne.
    Odtworzone zmienne sesyjne będą dostępne tylko w zmiennej
    $HTTP_SESSION_VARS.
  <example>
     <title>
      Rejestracja zmiennej z włączoną opcją <link
      linkend="ini.track-vars"><literal>track_vars</literal></link>
     </title>
     <programlisting role="php">
&lt;?php
session_register("count");
$HTTP_SESSION_VARS["count"]++;
?&gt;
     </programlisting>
    </example>
   </para>
   <para>
    Jeśli włączona jest opcja <link
    linkend="ini.register-globals"><literal>register_globals</literal></link>,
    wszystkie globalne zmienne mogą być zarejestrowane jako zmienne sesyjne a
    zmienne sesyjne będą odtworzone do odpowiadających im zmiennych
    globalnych.
  <example>
     <title>
      Rejestracja zminnych z włączoną opcją <link
      linkend="ini.register-globals"><literal>register_globals</literal></link>
     </title>
     <programlisting role="php">
&lt;?php
session_register("count");
$count++;
?&gt;
     </programlisting>
    </example>
   </para>
   <para>
    Jeśli włączone są obie opcje, <link
    linkend="ini.track-vars"><literal>track_vars</literal></link> i
    <link
    linkend="ini.register-globals"><literal>register_globals</literal></link>,
    globalne zmienne i wpisy w $HTTP_SESSION_VARS będą referencjami do tej
    samej wartości.
   </para>
   <para>
    Istnieją dwie metody propagacji identyfikatora sesji:
    <itemizedlist>
     <listitem>
      <simpara>
       Ciasteczka
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       Parametry URL'a
      </simpara>
     </listitem>
    </itemizedlist>
   </para>
   <para>
    Moduł sesji obsługuje obie metody. Ciasteczka są metodą optymalną, ale
    ponieważ nie są one pewne (klienci nie muszą ich akceptować), nie możęmy
    na nich polegać. Druga metora wstawia identyfikatory sesji bezpośrednio do
    URL'i.
   </para>
   <para>
    PHP może to robić 'przezroczyście' jeśli został skompilowany z opcją
    <link linkend="install.configure.enable-trans-sid">
    <literal>--enable-trans-sid</literal></link>. Jeśli włączysz tą opcję,
    względne URI zostaną automatycznie podmienione tak, aby zawierały
    identyfikator sesji. Możesz także użyć stałej <literal>SID</literal> która
    jest definiowana jeśli klient nie wysłał odpowiedniego ciastka. <literal>
    SID</literal> jest albo w postaci <literal>nazwa_sesji=id_sesji</literal>
    lub pustym stringiem.
   </para>
   <para>
    Poniższy przykład demonstruje jak zarejestrować zmienną i jak prawidłowo
    wstawić link do kolejnej strony korzystając ze stałej SID.
    <example>
     <title>Zliczanie ilości odwiedzin pojedyńczego użytkownika</title>
     <programlisting role="php">
&lt;?php
session_register ("count");
$count++;
?&gt;

Witaj gościu. Oglądasz tą stronę &lt;?php echo $count; ?&gt; raz.&lt;p&gt;

&lt;php?
# &lt;?=SID?&gt; jest konieczne do zachowania identyfikatora sesji jeśli
# użytkownik wyłączył ciastka
?&gt;

Aby kontynuować, &lt;A HREF="nextpage.php?&lt;?=SID?&gt;"&gt;kliknij tutaj&lt;/A&gt;
     </programlisting>
    </example>
   </para>
   <para>
    <literal>&lt;?=SID?&gt;</literal> nie jest konieczne jeśli przy kompilacji
    PHP użyta została opcja
    <link linkend="install.configure.enable-trans-sid">
    <literal>--enable-trans-sid</literal></link>.
   </para>
   <note>
    <para>
     PHP zakłada, że bezwzględne URLe odnoszą się do zewnętrznych serwisów,
     więc nie trzeba przekazywać SID, ponieważ istniałoby niebezpieczeństwo
     podkradania SIDów przez inny serwer.
    </para>
   </note>
   <para>
    Aby zaimplementować przechowywanie danych sesyjnych w bazie danych lub w
    dowolnej innej postaci, musisz użyć
    <function>session_set_save_handler</function> do stworzenia zestawu
    funkcji przechowujących dane.
   </para>
   <para>
    System zarządzania sesją obsługuje wiele opcji konfiguracyjnych, które
    możesz wstawić do swojego pliku php.ini. Oto ich krótki przegląd.
    <itemizedlist>
     <listitem>
      <simpara>
       <literal>session.save_handler</literal> definiuje nazwę procedury
       obsługi, która jest używana do przechowywania i odczytu danych
       skojarzonych z sesją. Domyślnie <literal>files</literal>.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.save_path</literal> definiuje argument, który jest
       przekazywany procedurze obsługi zapisu danych. Jeśli wybierzesz
       domyślną procedurę obsługi, jest to ścieżka gdzie tworzone będą pliki z
       danymi. Domyślnie <literal>/tmp</literal>.
      </simpara>
      <warning>
       <para>
        Jeśli w tej opcji ustawisz katalog, który jest ogólnie dostępny, jak
        na przykład <filename>/tmp</filename> (domyślna wartość), inni
        użytkownicy serwera będą w stanie przechwycić sesję przez pobranie
        listy plików z tego katalogu.
       </para>
      </warning>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.name</literal> określa nazwę sesji, która jest używana
       jako nazwa ciastka. Powinna zawierać tylko znaki alfanumeryczne.
       Domyślnie <literal>PHPSESSID</literal>.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.auto_start</literal> określa, czy moduł sesji
       rozpoczyna sesję na początku wywołania. Domyślnie
       <literal>0</literal> (wyłączony).
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.cookie_lifetime</literal> określa długość życia w
       sekundach ciastka przesyłanego do przeglądarki. Wartość 0 oznacza
       "dopóki przeglądarka nie została zamknięta". Domyślnie
       <literal>0</literal>.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.serialize_handler</literal> określa nazwę procedury
       obsługi, która zostanie użyta do serializacji/odserializacji danych.
       Obecnie obsługiwany jest wewnętrzny format PHP (nazwa <literal>php
       </literal> i WDDX (nazwa <literal>wddx</literal>). WDDX jest jedynym
       dostępnym formatem jeśli PHP zostało skompilowane z
       <link linkend="ref.wddx">obsługą WDDX</link>. Domyślnie
       <literal>php</literal>.
       </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.gc_probability</literal> określa prawdopodobieństwo w
       procentach rozpoczęcia procedury gc (garbage collection - zbieranie
       śmieci) przy każdym wywołaniu. Domyślnie <literal>1</literal>.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.gc_maxlifetime</literal> określa ilość sekund, po
       jakich dane będą rozpoznawane jako 'śmieci' i usuwane.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.referer_check</literal> zawiera podciąg, z którym
       HTTP_REFERER ma być sprawdzany. Jeśli HTTP_REFERER został wysłany przez
       klienta i nie zawierał podanego podciągu, identyfikator sesji podany
       przez takiego klienta zostanie uznany za nieważny. Domyślnie jest to
       ciąg pusty. 
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.entropy_file</literal> podaje ścieżkę do zewnętrznego
       zasobu (pliku), który będzie użyty jako dodatkowe źródło entropii w
       procesie tworzenia identyfikatora sesji. Przykłady to
       <literal>/dev/random</literal> lub
       <literal>/dev/urandom</literal>, które są dostępne na wielu systemach
       Unix.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.entropy_length</literal> określa liczbę bajtów, która
       będzie odczytana z pliku podanego powyżej. Domyślnie
       <literal>0</literal> (wyłączona).
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.use_cookies</literal> określa czy moduł będzie używał
       ciasteczek do przechowywania identyfikatora sesji po stronie klienta.
       Domyślnie <literal>1</literal> (włączona).
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.cookie_path</literal> określa ścieżkę która będzie
       podana w session_cookie. Domyślnie <literal>/</literal>.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.cookie_domain</literal> określa domenę która ma być
       podana w session_cookie. Domyślnie - pusta.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.cache_limiter</literal> określa metodę używaną do
       przechowywania stron sesyjnych w pamięci podręcznej 
       (nocache/private/private_no_expire/public). Domyślnie
       <literal>nocache</literal>.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.cache_expire</literal> określa czas życia w minutach
        stron sesyjnych zachowanych w pamięci podręcznej. Nie ma to efektu dla
        metody <literal>nocache</literal>. Domyślnie <literal>180</literal>
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>session.use_trans_sid</literal> określa czy będzie używana
       obsługa przezroczystego przekazywania identyfikatora sesji. Opcja brana
       pod uwagę tylko jeśli PHP zostało skompilowane z opcją
       <link linkend="install.configure.enable-trans-sid">
       <literal>--enable-trans-sid</literal></link>.
       Domyślnie <literal>1</literal> (włączona).
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       <literal>url_rewriter.tags</literal> określa które tagi HTML zostają
       przepisane w celu dopisania identyfikatora sesji jeśli włączona została
       opcja przezroczystego przekazywania identyfikatora sesji. Domyślnie
       <literal>a=href,area=href,frame=src,input=src,form=fakeentry</literal>
      </simpara>
     </listitem>
    </itemizedlist>
    <note>
     <para>
      Obsługa sesji została dodana w PHP 4.0.
     </para>
    </note>
   </para>
  </partintro>

  <refentry id="function.session-start">
   <refnamediv>
    <refname>session_start</refname>
    <refpurpose>Inicjalizuj dane sesji</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>bool <function>session_start</function></funcdef>
      <void/>
     </funcprototype>
    </funcsynopsis>
    <simpara>
     <function>session_start</function> tworzy sesję (lub odtwarza bieżącą
      w oparciu o identyfikator sesji przekazywany przez zmienne GET lub
      ciasteczko).</simpara>
    <simpara>
     Ta funkcja zawsze zwraca warotść &true;.
    </simpara>
    <note>
     <para>
      Ta funkcja została dodana w PHP 4.0.
     </para>
    </note>
   </refsect1>
  </refentry>

  <refentry id="function.session-destroy">
   <refnamediv>
    <refname>session_destroy</refname>
    <refpurpose>Niszczy wszystkie dane zarejestrowane w sesji</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>bool <function>session_destroy</function></funcdef>
      <void/>
     </funcprototype>
    </funcsynopsis>
    <simpara>
     <function>session_destroy</function> niszczy wszystkie dane skojarzone z
     bieżącą sesją.
    </simpara>
    <simpara>
     Funkcja ta zawraca &true; w przypadku sukcesu w niszczeniu danych sesji.
     W przeciwnym przypadku zwracana jest wartość &false;.
    </simpara>
   </refsect1>
  </refentry>

  <refentry id="function.session-name">
   <refnamediv>
    <refname>session_name</refname>
    <refpurpose>Pobierz i/lub ustaw nazwę bieżącej sesji</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>string <function>session_name</function></funcdef>
      <paramdef>string 
       <parameter><optional>nazwa</optional></parameter>
      </paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_name</function> zwraca nazwę bieżącej sesji. Jeśli
     podano parametr <parameter>name</parameter>, nazwa bieżącej sesji
     zostanie zmieniona na tą wartość.
    </para>
    <para>
     Nazwa sesji jest używana w identyfikatorze sesji w ciasteczkach i URLach.
     Powinna zawierać tylko znaki alfanumeryczne; powinna być krótka i
     treściwa (np. dla użytkowników z włączonymi ostrzeżeniami o
     ciasteczkach). Nazwa sesji jest przywracana do domyślnej wartości
     określonej w <literal>session.name</literal> na początku wywołania
     strony, a więc musisz wywołać <function>session_name</function> dla
     każdej strony (przed wywołaniem w niej <function>session_start</function>
     i <function>session_register</function>).
    </para>
    <example>
     <title>Przykłady <function>session_name</function></title>
     <programlisting role="php">
&lt;?php

# ustaw nazwę sesji na WebsiteID

$poprzednia_nazwa = session_name ("WebsiteID");

echo "Poprzednią nazwą sesji było $poprzednia_nazwa&lt;p&gt;";
?&gt;
     </programlisting>
    </example>
    <note>
     <para>
      Funkcja ta została dodana w PHP 4.0.
     </para>
    </note>
   </refsect1>
  </refentry>

  <refentry id="function.session-module-name">
   <refnamediv>
    <refname>session_module_name</refname>
    <refpurpose>Pobierz i/lub ustaw moduł bieżącej sesji</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>string <function>session_module_name</function></funcdef>
      <paramdef>string 
       <parameter><optional>moduł</optional></parameter>
      </paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_module_name</function> zwraca nazwę bieżącego modułu
     sesji. Jeśli podany został parametr <parameter>moduł</parameter>, użyty
     zostanie nowo podany moduł.
     <note>
      <para>
       Ta funkcja została dodana w PHP 4.0.
      </para>
     </note>
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-save-path">
   <refnamediv>
    <refname>session_save_path</refname>
    <refpurpose>Pobierz i/lub ustaw ścieżkę zapisu bieżącej sesji</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>string <function>session_save_path</function></funcdef>
      <paramdef>string 
       <parameter><optional>ścieżka</optional></parameter>
      </paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_save_path</function> zwraca ścieżkę do katalogu, który
     aktualnie jest używany do zapisu danych sesji. Jeśli podany został
     parametr <parameter>ścieżka</parameter>, zmieniona zostanie ścieżka
     zapisu danych.
     <note>
      <para>
       Na niektórych systemach operacyjnych możesz chciać podać ścieżkę do
       systemu plików, który lepiej obsługuje duże ilości małych plików. Na
       przykład na Linuksie reiserfs w takich warunkach ma lepszą wydajność
       niż ext2fs.
      </para>
     </note>
     <note>
      <para>
       Ta funkcja została dodana w PHP 4.0.
      </para>
     </note>
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-id">
   <refnamediv>
    <refname>session_id</refname>
    <refpurpose>Pobierz i/lub ustaw identyfikator bieżącej sesji</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>string <function>session_id</function></funcdef>
      <paramdef>string <parameter><optional>id</optional></parameter></paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_id</function> zwraca identyfikator sesji dla bieżącej
     sesji. Jeśli podany został parametr <parameter>id</parameter>, zostanie
     on użyty do zmiany identyfikatora bieżącej sesji.
    </para>
    <para>
     Do pobrania nazwy i identyfikatora bieżącej sesji moża być użyta także
     stała <systemitem>SID</systemitem>, która zawiera string odpowiedni do
     dodawania go do URLi.
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-register">
   <refnamediv>
    <refname>session_register</refname>
    <refpurpose>
     Zarejestruj jedną lub więcej zmiennych w bieżącej sesji
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>bool <function>session_register</function></funcdef>
      <paramdef>mixed <parameter>nazwa</parameter></paramdef>
      <paramdef>mixed
       <parameter><optional>...</optional></parameter></paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_register</function> jest funkcją o zmiennej liczbie
     argumentów, z których każdy może być albo stringiem zawierającym nazwę
     zmiennej lub tablicą zawierającą nazwy zmiennych lub inne tablice. Dla
     każdej napotkanej nazwy zmiennej, <function>session_register</function>
     rejestruje w bieżącej sesji globalną zmienną o danej nazwie.
    </para>
    <para>
     Funkcja ta zwraca wartość &true; jeśli zmienna została pomyślnie
     zarejestrowana w sesji.
    </para>
    <note>
     <para>
      W chwili obecnej niemożliwe jest zarejestrowanie w sesji zmiennych
      zawierających zasoby. Na przykład, nie możesz stworzyć połączenia do
      bazy danych i zachować identyfikator połączenia jako zmienną sesyjną i
      oczekować, że połączenie ciągle będzie aktywne po odtworzeniu danych
      sesji. Funkcję PHP, która zwraca zasoby, identyfikuje się przez
      zwracanie typu <literal>resource</literal> w definicji funkcji. Listę
      funkcji, które zwracają zasoby, można znaleźć w załączniku
      <link linkend="resource">typy zasobów</link>.
     </para>
    </note>
    <note>
     <para>
      Funkcja ta została dodana w PHP 4.0.
     </para>
    </note>
   </refsect1>
  </refentry>

  <refentry id="function.session-unregister">
   <refnamediv>
    <refname>session_unregister</refname>
    <refpurpose>
     Wyrejestruj zmienną z bieżącej sesji
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>bool <function>session_unregister</function></funcdef>
      <paramdef>string <parameter>nazwa</parameter></paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_unregister</function> wyrejestrowuje (zapomina)
     globalną zmienną o nazwie <parameter>nazwa</parameter> z bieżącej sesji.
    </para>
    <para>
     Funkcja ta zwraca wartość &true; jeśli zmienna została pomyślnie
     wyrejestrowana z sesji.
     <note>
      <para>
       Funkcja ta została dodana w PHP 4.0.
      </para>
     </note>
    </para>
   </refsect1>
  </refentry>
  
  <refentry id="function.session-unset">
   <refnamediv>
    <refname>session_unset</refname>
    <refpurpose>
     Zwolnij wszystkie zmienne sesyjne
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>void <function>session_unset</function></funcdef>
      <void/>
     </funcprototype>
    </funcsynopsis>
    <para>
     Funkcja <function>session_unset</function> zwalnia wszystkie zmienne
     sesyjne, które są aktualnie zarejestrowane.
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-is-registered">
   <refnamediv>
    <refname>session_is_registered</refname>
    <refpurpose>
     Sprawdź, czy zmienna jest zarejestrowana w sesji
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>bool <function>session_is_registered</function></funcdef>
      <paramdef>string <parameter>nazwa</parameter></paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_is_registered</function> zwraca wartość &true; jeśli w
     bieżącej sesji zarejestrowana jest zmienna o nazwie
     <parameter>nazwa</parameter>.
     <note>
      <para>
       Funkcja ta została dodana w PHP 4.0.
      </para>
     </note>
    </para>
   </refsect1>
  </refentry>
  
  <refentry id="function.session-get-cookie-params">
   <refnamediv>
    <refname>session_get_cookie_params</refname>
    <refpurpose>
     Pobierz parametry ciasteczka sesyjnego
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>
       array <function>session_get_cookie_params</function>
      </funcdef>
      <void/>
     </funcprototype>
    </funcsynopsis>
    <para>
     Funkcja <function>session_get_cookie_params</function> zwraca tablicę
     zawierającą informacje o bieżącym ciasteczku sesyjnym. Tablica zawiera
     poniższe informacje:
     <itemizedlist>
      <listitem>
       <simpara>
        "lifetime" -  Czas życia ciasteczka.
       </simpara>
      </listitem>
      <listitem>
       <simpara>
        "path" -  Ścieżka na której przechowywane są informacje.
       </simpara>
      </listitem>
      <listitem>
       <simpara>
        "domain" -  Domena ciasteczka.
       </simpara>
      </listitem>
     </itemizedlist>
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-set-cookie-params">
   <refnamediv>
    <refname>session_set_cookie_params</refname>
    <refpurpose>
     Ustaw parametry ciasteczka sesyjnego
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>void <function>session_set_cookie_params</function></funcdef>
      <paramdef>
       int <parameter>czas_życia</parameter>
      </paramdef>
      <paramdef>
       string <parameter><optional>ścieżka</optional></parameter>
      </paramdef>
      <paramdef>
       string <parameter><optional>domena</optional></parameter>
      </paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     Ustaw parametry ciasteczka pierwotnie zdefiniowane w pliku php.ini. Efekt
     działania tej funkcji widoczny jest tylko do końca działania skryptu.
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-decode">
   <refnamediv>
    <refname>session_decode</refname>
    <refpurpose>Dekoduje dane sesji ze stringu</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>bool <function>session_decode</function></funcdef>
      <paramdef>string <parameter>dane</parameter></paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_decode</function> dekofuje dane sesji zawarte w
     parametrze <parameter>dane</parameter>, ustawiając zmienne zachowane w
     sesji.
     <note>
      <para>
       Ta funkcja została dodana w PHP 4.0.
      </para>
     </note>
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-encode">
   <refnamediv>
    <refname>session_encode</refname>
    <refpurpose>
     Koduje dane bieżącej sesji do postaci stringu
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>string <function>session_encode</function></funcdef>
      <void/>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_encode</function> zwraca string zawierający zakodowane
     dane bieżącej sesji.
     <note>
      <para>
       Ta funkcja została dodana w PHP 4.0.
      </para>
     </note>
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-set-save-handler">
   <refnamediv>
    <refname>session_set_save_handler</refname>
    <refpurpose>
     Ustawia funkcje użytkownika do przechowywania sesji
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>void
       <function>session_set_save_handler</function>
      </funcdef>
      <paramdef>string
      <parameter>otwórz</parameter></paramdef><paramdef>string
      <parameter>zamknij</parameter></paramdef><paramdef>string
      <parameter>czytaj</parameter></paramdef><paramdef>string
      <parameter>zapisz</parameter></paramdef><paramdef>string
      <parameter>niszcz</parameter></paramdef><paramdef>string
      <parameter>gc</parameter></paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_set_save_handler</function> ustawia funkcje użytkownika
     do obsługi przechowywania sesji, które używane sa do zapisywania i
     odtwarzania danych skojarzonych z sesją. Jest to bardzo przydatne jeśli
     preferowany jest sposób przechowywania sesji inny niż ten, który jest
     dostarczany z PHP, np. przechowywanie danych sesji w lokalnej bazie
     danych.
    </para>
    <note>
     <para>
      W swoim pliku php.ini musisz ustawić opcję konfiguracji
      <parameter>session.save_handler</parameter> na
      <parameter>user</parameter> aby
      <function>session_set_save_handler</function> zadziałało.
     </para>
    </note>
    <note>
     <para>
      Procedura obsługi "zapisz" nie będzie wywołana dopóki strumień wyjściowy
      jest otwarty. W związku z tym, wyjście instrukcji debugowania zawartych
      w procedurze "zapisz" nigdy nie będzie widoczne w oknie przeglądarki.
      Jeśli konieczne jest wyjście debugowania, sugerowane jest zapisanie tego
      wyjścia do pliku.
     </para>
    </note>
    <para>
     Poniższy przykład opisuje metodę przechowywania danych sesyjnych w
     plikach, podobną do tej obsługiwanej wewnętrznie przez PHP. Przykład ten
     może być łatwo rozszerzony aby móc użyć go z twoją ulubioną bazą danych
     obsługiwaną przez PHP.
    </para>
    <para>
     <example>
      <title>
       Przykład <function>session_set_save_handler</function>
      </title>
      <programlisting role="php">
&lt;?php

function open ($save_path, $session_name) {
  global $sess_save_path, $sess_session_name;
       
  $sess_save_path = $save_path;
  $sess_session_name = $session_name;
  return(true);
}

function close() {
  return(true);
}

function read ($id) {
  global $sess_save_path, $sess_session_name;

  $sess_file = "$sess_save_path/sess_$id";
  if ($fp = @fopen($sess_file, "r")) {
    $sess_data = fread($fp, filesize($sess_file));
    return($sess_data);
  } else {
    return("");
  }

}

function write ($id, $sess_data) {
  global $sess_save_path, $sess_session_name;

  $sess_file = "$sess_save_path/sess_$id";
  if ($fp = @fopen($sess_file, "w")) {
    return(fwrite($fp, $sess_data));
  } else {
    return(false);
  }

}

function destroy ($id) {
  global $sess_save_path, $sess_session_name;
       
  $sess_file = "$sess_save_path/sess_$id";
  return(@unlink($sess_file));
}

/*********************************************
 * UWAGA: Musisz sam zaimplementować obsługę *
 * zbierania śmieci (gb)                     *
 *********************************************/
function gc ($maxlifetime) {
  return true;
}

session_set_save_handler ("open", "close", "read", "write", "destroy", "gc");

session_start();

// dalej normalnie używaj sesji

?&gt;
      </programlisting>
     </example>
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.session-cache-limiter">
   <refnamediv>
    <refname>session_cache_limiter</refname>
    <refpurpose>
     Pobierz i/lub ustaw bieżący ogranicznik pamięci podręcznej
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>string <function>session_cache_limiter</function></funcdef>
      <paramdef>string 
       <parameter><optional>ogranicznik</optional></parameter>
      </paramdef>
     </funcprototype>
    </funcsynopsis>
    <para>
     <function>session_cache_limiter</function> zwraca nazwę bieżącego
     ogranicznika pamięci podręcznej. Jeśli podany został parametr
     <parameter>ogranicznik</parameter>, nazwa bieżącego ogranicznego zostanie
     zmieniona na nową wartość.
    </para>
    <para>
     Ogranicznik pamięci podręcznej kontroluje nagłówki HTTP wysyłane do
     klienta. Nagłówki te ustalają zasady, według których zawartość strony
     może być przechowywana w pamięci podręcznej przeglądarki. Ustawiając
     ogranicznik pamięci podręcznej na przykład na<literal>nocache</literal>,
     zabronimy jakiekolwiek zachowywanie strony po stronie klienta. Wartość
     <literal>public</literal> pozwoli na takie przechywanie. Ogranicznikiem
     może być też <literal>private</literal>, który jest troszkę bardziej
     restrykcyjny niż <literal>public</literal>.
    </para>
    <para>
     W trybie <literal>private</literal>, nagłówek <literal>Expire</literal>,
     który jest wysyłany do klienta, może spowodować nieoczekiwane działanie
     niektórych przeglądarem, między innymi Mozilli. Możesz uniknąć tego
     problemu używając trybu <literal>private_no_expire</literal>. Nagłówek
     <literal>Expire</literal> nie jest w tym wypadku wysyłany do klienta.
    </para>
    <note>
     <para>
      <literal>private_no_expire</literal> zostało dodane w PHP 4.2.0dev.
     </para>
    </note>
    <para>
     Ogranicznik pamięci podręcznej w momencie wywołania skryptu jest zerowany
     do wartości domyślnej przechowywanej w 
     <literal>session.cache_limiter</literal>. W związku z tym niezbędne jest
     wywołanie <function>session_cache_limiter</function> dla każdego
     wywołania skryptu (i przed wywołaniem <function>session_start</function>).
    </para>
    <example>
     <title>Przykłady <function>session_cache_limiter</function></title>
     <programlisting role="php">
&lt;?php

# Ustaw ogranicznik pamięci podręcznej na 'private'

session_cache_limiter('private');
$cache_limiter = session_cache_limiter();

echo "Ogranicznikiem pamięci podręcznej jest teraz $cache_limiter&lt;p&gt;";
?&gt;
     </programlisting>
    </example>
    <note>
     <para>
      Funkcja ta została dodana w PHP 4.0.3.
     </para>
    </note>
   </refsect1>
  </refentry>

  <refentry id="function.session-write-close">
   <refnamediv>
    <refname>session_write_close</refname>
    <refpurpose>Zapisz dane i zakończ sesję</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>void <function>session_write_close</function></funcdef>
      <void/>
     </funcprototype>
    </funcsynopsis>
    <para>
     Zakończ bieżącą sesję i zachowaj dane sesji.
    </para>
    <para>
     Dane sesji są zazwyczaj przechowywane do czasu zakończenie działania
     skryptu bez konieczności wywołania <function>session_write_close
     </function>, ale ponieważ dane sesji są blokowane w celu zapobieżenia
     równoległym zapisom, tylko jeden skrypt może operować na sesji w danej
     chwili. Używając ramek HTMLowych razem z sesjami napotkasz problemy
     związane z jednoczesnym korzystaniem z jednej sesji przez kilka skryptów.
     Możesz zmniejszyć czas niezbędny do wczytania wszystkich ramek przez
     kończenie sesji jak tylko wykonane są wszystkie zmiany w zmiennych
     sesyjnych.
    </para>
<!-- wykomentowane do czasu ostatecznej decyzji o implementacji
    <para>
     Zobacz także: <function>session_readonly</function>.
    </para>
-->
   </refsect1>
  </refentry>

<!-- wykomentowane do czasu ostatecznej decyzji o implementacji
  <refentry id="function.session-readonly">
   <refnamediv>
    <refname>session_readonly</refname>
    <refpurpose>Rozpocznij sesję - reinicjalizuj zamrożone zmienne, ale nie
    nie zapisuj zmian po zakończeniu wywołania</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
    <funcsynopsis>
     <funcprototype>
      <funcdef>void <function>session_readonly</function></funcdef>
      <void/>
     </funcprototype>
    </funcsynopsis>
    <para>
     Odczytaj dane sesji bez blokowania tych danych. Nie jest możliwa zmiana
     danych sesyjnych, ale zwiększona jest wydajność ramek HTML.
    </para>
   </refsect1>
  </refentry>
-->
 </reference>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

<?xml version="1.0" encoding="iso-8859-2"?>
<!-- EN-Revision: 1.36 Maintainer: Qrak Status: ready -->
<!-- $Revision: 1.13 $ -->
 <reference id="ref.http">
  <title>HTTP</title>
  <titleabbrev>HTTP</titleabbrev>

  <partintro>
   <simpara>
    Te funkcje pozwalają ci operować na danych wysyłanych do przeglądarki
    internetowej na poziomie protokołu HTTP.
   </simpara>
  </partintro>

  <refentry id="function.header">
   <refnamediv>
    <refname>header</refname>
    <refpurpose>Wysyła surowy nagłowek HTTP</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
     <methodsynopsis>
      <type>int</type><methodname>header</methodname>
      <methodparam><type>string</type><parameter>treść_nagłówka</parameter></methodparam>
      <methodparam choice="opt"><type>bool</type><parameter>zamień</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>header</function> służy do wysłania surowego
     nagłówka <acronym>HTTP</acronym>.
     Zajrzyj do <ulink url="&spec.http1.1;">Specyfikacji HTTP 1.1</ulink>
     aby dowiedzieć się więcej na temat nagłówków <acronym>HTTP</acronym>.
    </para>
    <para>
     Opcjonalny agrument <parameter>zamień</parameter> określa, czy funkcja
     ma zastąpić nagłówek tego samego typu przygotowany przez serwer,
     czy dodać jeszcze jeden. Domyślnie, oryginalny
     nagłówek zostanie zastąpiony, ale jeśli ustawisz ten argument na &false;,
     to nowy nagłówek zostanie dodany do już istniejących. Na przykład:
     <informalexample>
      <programlisting role="php">
<![CDATA[      
header('WWW-Authenticate: Negociate');
header('WWW-Authenticate: NTLM',false);
]]>
      </programlisting>
     </informalexample>
    </para>
    <para>
     W PHP są dwa specjalne wywołania <function>header</function>.
     Pierwszym z nich jest &quot;Location&quot;. To wywołanie nie tylko
     wysyła ten nagłówek do przeglądarki, ale także wysyła do przeglądarki
     status przekierowania <literal>REDIRECT</literal> (302). 
     <informalexample>
      <programlisting role="php">
<![CDATA[
header("Location: http://www.php.net/"); /* Przekieruj przeglądarkę 
                                            na stronę główną PHP */
exit;                 /* Upewnij się, że kod poniżej nie zostanie wykonany 
                         po przekierowaniu. */
]]>
      </programlisting>
     </informalexample>
    </para>
    <note>
     <para>
      Protokół HTTP 1.1 wymaga bezwzględnego <acronym>URI</acronym> w nagłówku
      <ulink url="&spec.http1.1;/rfc2616-sec14.html#sec14.30">Location:</ulink>
      włącznie z określeniem protokołu, nazwy hosta i bezwzględnej scieżki
      dostępu, ale niektóre klienty akceptują względne <acronym>URI</acronym>.
      Zwykle używa się
      $HTTP_SERVER_VARS['HTTP_HOST'], $HTTP_SERVER_VARS['PHP_SELF'] i
      funkcji <function>dirname</function> by wygenerować bezwględnego 
      <acronym>URI</acronym>:
      <informalexample>
       <programlisting>
<![CDATA[
header("Location: http://".$HTTP_SERVER_VARS['HTTP_HOST']
                       ."/".dirname($HTTP_SERVER_VARS['PHP_SELF'])
                       ."/".$relative_url);
]]>
       </programlisting>
      </informalexample>
     </para>
    </note>
    <para>
     Drugim specjalnym wywołaniem funkcji jest każdy nagłówek zaczynający się
     od ciągu znaków &quot;<literal>HTTP/</literal>&quot; (wielkość liter nie
     gra roli), którego używa się do określenia, jaki kod statusu HTTP ma zostać
     wysłany.
     Na przykład, jeśli skonfigurowałeś Apache'a tak, że skrypt PHP obsługuje
     zapytania do nieistniejących plików (za pomocą dyrektywy 
     <literal>ErrorDocument</literal>), powinieneś zwrócić uwagę, aby skrypt
     generował właściwy kod statusu zapytania HTTP.
     <informalexample>
      <programlisting role="php">
<![CDATA[
header("HTTP/1.0 404 Not Found");
]]>
      </programlisting>
     </informalexample>
    </para>
    <note>
      <para>
       W PHP 3 działa to tylko wtedy, kiedy PHP jest skompilowane jako moduł
       serwera Apache. Taki sam efekt można osiągnąć za pomocą nagłówka
       <literal>Status</literal>.
       <informalexample>
        <programlisting role="php">
<![CDATA[
header("Status: 404 Not Found");
]]>
        </programlisting>
       </informalexample>
      </para>
     </note>
    <para>
     Skrypty PHP często służą do generowania dynamiczej treści, która
     nie może być buforowana przez klienta czy serwer proxy. Pamięć cache
     (bufor) w większości tych urządzeń da się wyłączyć dzięki:
     <informalexample>
      <programlisting role="php">
<![CDATA[
header ("Expires: Mon, 26 Jul 1997 05:00:00 GMT");    // data w przeszłości
header ("Last-Modified: " . gmdate("D, d M Y H:i:s") . " GMT"); 
                                                      // ciągle modyfikowany
header("Cache-Control: no-store, no-cache, must-revalidate");  // HTTP/1.1
header("Cache-Control: post-check=0, pre-check=0", false);
header("Pragma: no-cache");                          // HTTP/1.0
]]>
      </programlisting>
     </informalexample>
     <note>
      <para>
       Możesz zaobserwować, że strony nie są buforowane, nawet jeśli
       nie użyłeś wszystkich ww. nagłówków. Jest wiele sposobów, w jakie
       użytkownicy mogą skonfigurować swoje przeglądarki, aby zmienić
       standardowy sposób buforowania. Przez wysłanie powyższych nagłówków,
       powinno się udać ominąć jakiekolwiek ustawienia pozwalające
       na zbuforowanie wyniku pracy twojego skryptu.
      </para>
      <para>
       Dodatkowo, <function>session_cache_limiter</function> i dyrektywa
       konfiguracyjna <literal>session.cache_limiter</literal> służą
       do automatycznego generowania nagłówków związanych z bufurowaniem,
       kiedy sesje są w użyciu.
      </para>
     </note>
    </para>
    <para>
     Pamiętaj, że <function>header</function> może być wywoływana 
     jedynie do momentu nim zostanie wysłana jakakolwiek treść, tzn. 
     znaczniki HTML, puste linie lub wynik pracy PHP. Jest to bardzo
     częsty błąd, gdzie skrypty z funkcjami <function>include</function>, 
     <function>require</function> itp. mają spacje albo puste linie przed
     wywołaniem funkcji <function>header</function>. 
     Problem ten pojawia się również w skryptach opartych na
     pojedynczym pliku PHP/HTML.
     <informalexample>
      <programlisting role="php">
<![CDATA[
&lt;?php require("user_logging.inc") ?&gt;


&lt;?php header ("Content-Type: audio/x-pn-realaudio"); ?&gt;
// skrypt nie działa - zauważ puste linie pomiędzy instrukcjami
]]>
      </programlisting>
     </informalexample>
     <note>
      <para>
       W PHP 4 można użyć buforowania wyjścia aby ominąć ten problem.
       Wszystko, co skrypt wyśle do przeglądarki zostanie zatrzymane na
       serwerze do momentu, kiedy pojawi się instrukcja wysłania danych.
       Można to zrobić za pomocą funkcji <function>ob_start</function> i
       <function>ob_end_flush</function>, lub ustawiając dyrektywę kofiguracyjną
       <literal>output_buffering</literal> w pliku <filename>php.ini</filename>
       lub w plikach konfiguracyjnych serwera.
      </para>
     </note>
    </para>
    <para>
     Aby użytkownik został monitowany o zapisanie wysyłanych danych,
     takich jak np. wygenerowany plik PDF, można użyć nagłówka <ulink
     url="&url.rfc2183;">Content-Disposition</ulink> aby podać zalecaną nazwę
     pliku i zmusić przeglądarkę do wyświetlenia okienka Zapisz jako.
     <informalexample>
      <programlisting role="php">
<![CDATA[
&lt;?php
header("Content-type: application/pdf");
header("Content-Disposition: attachment; filename=downloaded.pdf");

/* ... treść pliku pdf ... */
]]>
      </programlisting>
     </informalexample>
     <note>
      <para>
       W Microsoft Internet Explorer 4.01 jest błąd, który uniemożliwia
       wykorzystanie tego mechanizmu. Nie ma na to rozwiązania. Błąd, który
       zahacza o ten mechanizm, jest także w Microsoft Internet Explorer 5.5,
       jednak da się go ominąć aktualizując przeglądarkę poprzez Service Pack 2
       lub późniejszy.
      </para>
     </note>
    </para>    
    <para>
     Zobacz też <function>headers_sent</function>,
     <function>setcookie</function>, i rozdział 
     <link linkend="features.http-auth">Autoryzacja HTTP</link>.
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.headers-sent">
   <refnamediv>
    <refname>headers_sent</refname>
    <refpurpose>Zwraca &true; jeśli nagłówki zostały już wysłane</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
     <methodsynopsis>
      <type>bool</type><methodname>headers_sent</methodname>
      <void/>
     </methodsynopsis>
    <para>
     <function>headers_sent</function> zwraca &true; jeśli nagłówki HTTP już
     zostały wysłane, lub &false; w przeciwnym wypadku.
    </para>
    <para>
     Zobacz też <function>header</function>
    </para>
   </refsect1>
  </refentry>

  <refentry id="function.setcookie">
   <refnamediv>
    <refname>setcookie</refname>
    <refpurpose>Wysyła ciasteczko</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Opis</title>
     <methodsynopsis>
      <type>int</type><methodname>setcookie</methodname>
      <methodparam><type>string</type><parameter>nazwa</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>wartość</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>data_ważności</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>ścieżka</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>domena</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>bezpieczne</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>setcookie</function> określa ciasteczko (ang. cookie) do
     wysłania z nagłówkami HTTP. Ciasteczko musi być wysłane <emphasis>zanim
     </emphasis> jakiekolwiek inne nagłówki zostaną wysłane (to jest
     ograniczenie ciasteczek, nie PHP). To wymaga od ciebie umieszczenia
     wywołań tej funkcji przed znacznikami <literal>&lt;html></literal> czy
     <literal>&lt;head></literal>.
    </para>
    <para>
     Wszystkie argumenty poza <parameter>nazwa</parameter> są opcjonalne.
     Jeśli tylko argument nazwa jest obecny, ciasteczko o takie nazwie zostanie
     usunięte z klienta. Możesz też opuścić argumenty za pomocą pustego łańcucha
     (<emphasis>&quot;&quot;</emphasis>). Argumenty <parameter>data_ważności
     </parameter> i <parameter>bezpieczne</parameter> są liczbami całkowitymi
     i nie można ich opuścić wstawiając pusty łańcuch. Zamiast niego użyj
     liczby zero (<emphasis>0</emphasis>). Argument 
     <parameter>data_ważności</parameter> 
     jest regularnym uniksowym znacznikiem czasu, takim jak
     zwracany przez funkcje <function>time</function> lub <function>mktime
     </function>. Argument <parameter>bezpieczne</parameter> oznacza, że
     ciasteczko może być przekazywane tylko poprzez bezpieczne połączenie HTTPS.
    </para>
    <para>
     Częste pułapki:
     <itemizedlist>
      <listitem>
       <simpara>
        Ciasteczka nie będą widziane do następnego przeładowania strony
        dla której mają być widoczne.
       </simpara>
      </listitem>
      <listitem>
       <simpara>
        Ciasteczko może być usunięte tylko z tymi parametrami, z jakimi je
        ustawiono.
       </simpara>
      </listitem>
     </itemizedlist>
    </para>
    <simpara>
     W PHP 3, wielokrotne wywołania <function>setcookie</function> w jednym
     skrypcie wykonywane były w odwrotnej kolejności. Jeśli chcesz usunąć jedno
     ciastko przed wprowadzeniem kolejnego, powinieneś umieścić wprowadzenie
     nowego przed usunęciem starego. W PHP 4, wielokrotne wywołania 
     <function>setcookie</function> wykonywane są we właściwej kolejności.
    </simpara>
    <para>
     Parę przykładów - jak wysyłać ciasteczka:
     <example>
      <title><function>setcookie</function> wysyła przykładowe ciasteczka
      </title>
      <programlisting role="php">
<![CDATA[
setcookie ("TestCookie", $value);
setcookie ("TestCookie", $value,time()+3600);  /* traci ważność za godzinę */
setcookie ("TestCookie", $value,time()+3600, "/~rasmus/", ".utoronto.ca", 1);
]]>
      </programlisting>
     </example>
    </para>
    <para>
     Aby skasować ciasteczko, należy ustawić datę ważności na datę w
     przeszłości, co uruchomi w przeglądarce mechanizm kasowania ciasteczek.
     A teraz, jak usunąć ciasteczka z poprzedniego przykładu:
     <example>
      <title>Kasowanie ciasteczek</title>
      <programlisting role="php">
<![CDATA[
// ustaw datę ważności na godzinę wstecz
setcookie ("TestCookie", "", time() - 3600);
setcookie ("TestCookie", "", time() - 3600, "/~rasmus/", ".utoronto.ca", 1);
]]>
      </programlisting>
     </example>
    </para>
    <para>
     Proszę zwrócić uwagę, że zawartość ciasteczka jest automatycznie kodowana
     do formatu
     URL przy wysyłaniu, a po odebraniu automatycznie dekodowana i przypisywana
     do zmiennej o tej samej nazwie co ciasteczko. Aby poznać zawartość
     przykładowego ciasteczka ze skryptu, należy zastosować poniższy przykład:
     <informalexample>
      <programlisting role="php">
<![CDATA[
echo $TestCookie;
echo $HTTP_COOKIE_VARS["TestCookie"];
]]>
      </programlisting>
     </informalexample>
    </para>
    <para>
     Można też tworzyć tablice złożone z ciasteczek za pomocą zapisu tablicowego
     w nazwie ciasteczka. Powoduje to utworzenie tylu ciasteczek, ile jest
     elemetów tablicy, a po otrzymaniu takiego ciasteczka, jego wartości
     umieszczane są w tablicy o nazwie takiej jak ciasteczko.
     <informalexample>
      <programlisting role="php">
<![CDATA[
setcookie ("cookie[three]", "cookiethree");
setcookie ("cookie[two]", "cookietwo");
setcookie ("cookie[one]", "cookieone");
if (isset ($cookie)) {
    while (list ($name, $value) = each ($cookie)) {
        echo "$name == $value&lt;br>\n";
    }
}
]]>
      </programlisting>
     </informalexample>
    </para>
    <para>
     Więcej informacji na temat ciasteczek znajduje się w specyfikacji
     Netscape'a, na stronie <ulink
     url="&spec.cookies;">&spec.cookies;</ulink>.
    </para>
    <simpara>
     Microsoft Internet Explorer 4 z zainstalowanym Service Pack 1 nie radzi
     sobie poprawnie z ciasteczkami, które mają ustawiony parametr
     <parameter>ścieżka</parameter>. 
    </simpara>
    <simpara>
     Netscape Communicator 4.05 i Microsoft Internet Explorer 3.x niepoprawnie
     obsługują ciasteczka, w których <parameter>ścieżka</parameter> i 
     <parameter>data_ważności</parameter> nie są ustawione.
    </simpara>
   </refsect1>
  </refentry>

 </reference>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->


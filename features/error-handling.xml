<?xml version="1.0" encoding="iso-8859-2"?>
<!-- EN-Revision: 1.26 Maintainer: slawek Status: ready -->
<!-- $Revision: 1.3 $ -->
 <chapter xml:id="features.error-handling" xmlns="http://docbook.org/ns/docbook">
  <title>Obsługa błędów</title>
 
  <para>
   W PHP istnieje kilka rodzajów błędów i ostrzeżeń. Oto one:

   <table>
    <title>Rodzaje błędów PHP</title>
    <tgroup cols="4">
     <thead>
      <row>
       <entry>Wartość</entry>
       <entry>Stała</entry>
       <entry>Opis</entry>
       <entry>Uwagi</entry>
      </row>
     </thead>
     <tbody>
      <row>
       <entry>1</entry>
       <entry>E_ERROR</entry>
       <entry>krytyczne błędy podczas wykonywania</entry>
       <entry></entry>
      </row>
      <row>
       <entry>2</entry>
       <entry>E_WARNING</entry>
       <entry>ostrzeżenia podczas wykonania (nie błędy krytyczne)</entry>
       <entry></entry>
      </row>
      <row>
       <entry>4</entry>
       <entry>E_PARSE</entry>
       <entry>błędy składniowe podczas kompilacji</entry>
       <entry></entry>
      </row>
      <row>
       <entry>8</entry>
       <entry>E_NOTICE</entry>
       <entry>uwagi podczas wykonania (mniej poważne niż ostrzeżenia)</entry>
       <entry></entry>
      </row>
      <row>
       <entry>16</entry>
       <entry>E_CORE_ERROR</entry>
       <entry>krytyczne błędy podczas uruchamiania PHP</entry>
       <entry>tylko PHP 4</entry>
      </row>
      <row>
       <entry>32</entry>
       <entry>E_CORE_WARNING</entry>
       <entry>ostrzeżenia (nie błędy krytyczne) podczas uruchamiania PHP</entry>
       <entry>tylko PHP 4</entry>
      </row>
      <row>
       <entry>64</entry>
       <entry>E_COMPILE_ERROR</entry>
       <entry>krytyczne błędy podczas kompilacji</entry>
       <entry>tylko PHP 4</entry>
      </row>
      <row>
       <entry>128</entry>
       <entry>E_COMPILE_WARNING</entry>
       <entry>ostrzeżenia podczas kompilacji (nie błędy krytyczne)</entry>
       <entry>tylko PHP 4</entry>
      </row>
      <row>
       <entry>256</entry>
       <entry>E_USER_ERROR</entry>
       <entry>komunikat błędu wywołany przez użytkownika</entry>
       <entry>tylko PHP 4</entry>
      </row>
      <row>
       <entry>512</entry>
       <entry>E_USER_WARNING</entry>
       <entry>komunikat ostrzeżenia wywołany przez użytkownika</entry>
       <entry>tylko PHP 4</entry>
      </row>
      <row>
       <entry>1024</entry>
       <entry>E_USER_NOTICE</entry>
       <entry>uwaga wywołana przez użytkownika</entry>
       <entry>tylko PHP 4</entry>
      </row>
      <row>
       <entry></entry>
       <entry>E_ALL</entry>
       <entry>wszystkie powyższe, jeśli obsługiwane</entry>
       <entry></entry>
      </row>
     </tbody>
    </tgroup>
   </table>
  </para>

  <para>
   Powyższe wartości (zarówno liczbowe jak i symboliczne) tworzą maskę bitową,
   określającą, które błędy mają być zgłaszane. Możesz użyć
   <link linkend="language.operators.bitwise">operatorów bitowych</link>, by
   połączyć te wartości, lub zamaskować niektóre typy błędów. Należy podkreślić,
   że jedynie operatory '|', '~', '!', i '&amp;' będą akceptowane przez
   &php.ini;, natomiast żaden z nich nie będzie akceptowany
   przez <filename>php3.ini</filename>.
  </para>
  <para>
   W PHP4, <link linkend="ini.error-reporting">error_reporting</link>
   (zgłaszanie błędów) jest domyślnie ustawione na
   <literal>E_ALL &amp; ~E_NOTICE</literal>, co powoduje wyświetlanie wszystkich
   błędów i ostrzeżeń, z wyjątkiem kategorii E_NOTICE. W PHP3, domyślne
   ustawienie <literal>(E_ERROR | E_WARNING | E_PARSE)</literal>, powoduje
   dokładnie to samo. Biorąc jednak pod uwagę, iż <filename>php3.ini</filename>
   nie obsługuje stałych, <link linkend="ini.error-reporting">error_reporting</link>
   musi zostać podane liczbowo, dlatego wynosi <literal>7</literal>.
  </para>

  <para>
   Początkowa wartość może być ustawiona dyrektywą <link
   linkend="ini.error-reporting">error_reporting</link> w pliku ini, lub w
   pliku <filename>httpd.conf</filename> serwera Apache przy użyciu dyrektywy
   php_error_reporting (lub php3_error_reporting w PHP3), lub z poziomu
   samego skryptu przy użyciu funkcji <function>error_reporting</function>.
  </para>
  
  <warning>
   <para>
    W przypadku aktualizowania skryptów bądź serwera z PHP 3 do PHP 4, należy
    sprawdzić ustawienia zgłaszania błędów, oraz wywołania funkcji
    <function>error_reporting</function>. Istnieje bowiem możliwość
    przypadkowego zablokowania zgłaszania nowych typów błędów, w szczególności
    E_COMPILE_ERROR. Może to prowadzić do wyświetlania pustych dokumentów, bez
    jakiejkolwiek informacji o błędzie, ani miejscu, gdzie należy szukać
    problemu.
   </para>
  </warning>
    
  <para>
   Wszystkie <link linkend="language.expressions">wyrażenia PHP</link> mogą być
   poprzedzane znakiem "@", który wyłącza zgłaszanie błędów dla następującego
   po nim wyrażenia. Jeśli podczas przetwarzania wyrażenia wystąpi błąd, a
   włączona jest opcja <link linkend="ini.track-errors">track_errors</link>,
   to komunikat błędu zostanie umieszczony w zmiennej globalnej
   <literal>$php_errormsg</literal>.
  </para>

  <note>
   <para>
    <link linkend="language.operators.errorcontrol">Operator kontroli błędów
    @</link> nie zablokuje komunikatu, który jest wynikiem błędu składniowego.
   </para>
  </note>

  <warning>
   <para>
    <link linkend="language.operators.errorcontrol">Operator kontroli błędów
    @</link> zablokuje również zgłaszanie błędów krytycznych, powodujących
    przerwanie wykonywania skryptu. Jeśli zatem użyjesz operatora 
    <link linkend="language.operators.errorcontrol">@</link> do wyłączenia
    pokazywania błędów konkretnej funkcji, która bądź nie istnieje, bądź
    popełniono błąd przy wpisywaniu jej nazwy, skrypt przerwie działanie w tym
    miejscu, bez poinformowania o przyczynach.
   </para>
  </warning>

  <para>
   Poniżej zamieszczamy przykład wykorzystania obsługi błędów w PHP. Tworzymy
   funkcję obsługi błędów, która zapisuje informacje do pliku (używając formatu
   XML) i w przypadku wystąpienia błędu krytycznego, wysyła maila do
   programisty.
   <example>
    <title>Wykorzystanie obsługi błędów</title>
    <programlisting role="php">
<![CDATA[
<?php
// będziemy korzystać z własnej obsługi błędów
error_reporting(0);

// zdefiniowana przez nas funkcja obsługi błędów
function userErrorHandler ($errno, $errmsg, $filename, $linenum, $vars) {
    // czas wystąpienia błędu
    $dt = date("Y-m-d H:i:s (T)");

    // tablicja asocjacyjna rodzajów błędów.
    // w rzeczywistości będziemy brać pod
    // uwagę błędy are 2,8,256,512 i 1024
    $errortype = array (
                1   =>  "Błąd",
                2   =>  "Ostrzeżenie",
                4   =>  "Błąd składni",
                8   =>  "Uwaga",
                16  =>  "Błąd PHP (core)",
                32  =>  "Ostrzeżenie PHP (core)",
                64  =>  "Błąd kompilacji",
                128 =>  "Ostrzeżenie kompilatora",
                256 =>  "Błąd użytkownika",
                512 =>  "Ostrzeżenie użytkownika",
                1024=>  "Uwaga użytkownika"
                );
    // błędy dla których zapisywany będzie stan zmiennych
    $user_errors = array(E_USER_ERROR, E_USER_WARNING, E_USER_NOTICE);
    
    $err = "<errorentry>\n";
    $err .= "\t<datetime>".$dt."</datetime>\n";
    $err .= "\t<errornum>".$errno."</errornum>\n";
    $err .= "\t<errortype>".$errortype[$errno]."</errortype>\n";
    $err .= "\t<errormsg>".$errmsg."</errormsg>\n";
    $err .= "\t<scriptname>".$filename."</scriptname>\n";
    $err .= "\t<scriptlinenum>".$linenum."</scriptlinenum>\n";

    if (in_array($errno, $user_errors))
        $err .= "\t<vartrace>".wddx_serialize_value($vars,"Variables")."</vartrace>\n";
    $err .= "</errorentry>\n\n";
    
    // dla testów
    // echo $err;

    // zapisz w dzienniku błędów i wyślij mailem informację o błędzie użytkownika
    error_log($err, 3, "/usr/local/php4/error.log");
    if ($errno == E_USER_ERROR)
        mail("phpdev@example.com","Krytyczny błąd użytkownika",$err);
}


function distance ($vect1, $vect2) {
    if (!is_array($vect1) || !is_array($vect2)) {
        trigger_error("Niepoprawne parametry, oczekiwano tablic", E_USER_ERROR);
        return NULL;
    }

    if (count($vect1) != count($vect2)) {
        trigger_error("Wektory muszą mieć jednakowy rozmiar", E_USER_ERROR);
        return NULL;
    }

    for ($i=0; $i<count($vect1); $i++) {
        $c1 = $vect1[$i]; $c2 = $vect2[$i];
        $d = 0.0;
        if (!is_numeric($c1)) {
            trigger_error("Współrzędna $i w wektorze 1 nie jest liczbą, przyjmuję zero",
                            E_USER_WARNING);
            $c1 = 0.0;
        }
        if (!is_numeric($c2)) {
            trigger_error("Współrzędna $i w wektorze 2 nie jest liczbą, przyjmuję zero", 
                            E_USER_WARNING);
            $c2 = 0.0;
        }
        $d += $c2*$c2 - $c1*$c1;
    }
    return sqrt($d);
}

$old_error_handler = set_error_handler("userErrorHandler");

// niezdefiniowana stała, generuje błąd
$t = I_AM_NOT_DEFINED;

// definicja kilku wektorów
$a = array(2,3,"foo");
$b = array(5.5, 4.3, -1.6);
$c = array (1,-3);

// generujemy błąd użytkownika
$t1 = distance($c,$b)."\n";

// ... i jeszcze jeden
$t2 = distance($b,"nie jestem tablicą")."\n";

// generujemy ostrzeżenie
$t3 = distance($a,$b)."\n";

?>
]]>
    </programlisting>
   </example>
   To jedynie prosty przykład, ilustrujący sposób użycia
   <link linkend="ref.errorfunc">Funkcji obsługi błędów i logowania</link>.
  </para>

  <para>
   Patrz także: <function>error_reporting</function>,
   <function>error_log</function>,
   <function>set_error_handler</function>,
   <function>restore_error_handler</function>, 
   <function>trigger_error</function>,
   <function>user_error</function>
  </para>

 </chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

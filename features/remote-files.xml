<?xml version="1.0" encoding="iso-8859-2"?>
<!-- $Revision: 1.3 $ -->
 <chapter id="features.remote-files">
  <title>Korzystanie ze zdalnych plików</title>

  <para>
   Jeżeli PHP zostanie skonfigurowany z włączoną obsługą
   &quot;URL fopen wrapper&quot;, będzie można używać adresów HTTP i FTP
   w większości funkcji, które jako parametr przyjmują nazwę pliku,
   włączając w to instrukcje <function>require</function> i
   <function>include</function>. Obsługa &quot;URL fopen wrapper&quot;
   jest włączona domyślnie, chyba, że dodasz opcję
   <option>--disable-url-fopen-wrapper</option> do ./configure
   (w wersjach do 4.0.3) lub ustawisz <parameter>allow_url_fopen</parameter>
   na <literal>off</literal> w pliku php.ini (w nowszych wersjach).
  </para>
  <para>
   <note>
    <para>
     Nie możesz używać zdalnych plików w <function>include</function> i
     <function>require</function> w systemie Windows.
    </para>
   </note>
  </para>
  <para>
   Możesz wykorzystać tę własność aby otworzyć plik na zdalnym serwrze,
   przetworzyć jego zawartość i użyć wyników w zapytaniu do bazy danych,
   lub po prostu wyświetlić plik dostosowując jego wygląd do swojej strony.
  </para>
  <para>
   <example>
    <title>Pobieranie tytułu zdalnej strony</title>
    <programlisting role="php">
<![CDATA[
<?php
$file = fopen ("http://www.php.net/", "r");
if (!$file) {
    echo "<p>Nie można otworzyć zdalnego pliku.\n";
    exit;
}
while (!feof ($file)) {
    $line = fgets ($file, 1024);
    /* Zadziała tylko wtedy, gdy tytuł i jego znaczniki są w tej samej linii */
    if (eregi ("<title>(.*)</title>", $line, $out)) {
        $title = $out[1];
        break;
    }
}
fclose($file);
?>
]]>
    </programlisting>
   </example>
  </para>
  <para>
   Możesz również zapisywać pliki na serwerach FTP, jeśli połączysz się
   jako użytkownik z odpowiednimi prawami dostepu i jeśli plik wcześniej
   nie istniał. Aby połączyć się jako użytkownik inny niż 'anonymous',
   musisz przesłać nazwę użytkonika (i najprawdopodobniej hasło) w URLu,
   np. 'ftp://uzytkownik:haslo@ftp.example.com/sciezka/do/pliku'. Możesz
   użyć tej samej składni przy dostępie do plików przez HTTP, jeśli
   wymagana jest autoryzacja.
  </para>
  <para>
   <example>
    <title>Zapisywanie danych na zdalnym serwerze.</title>
    <programlisting role="php">
<![CDATA[
<?php
$file = fopen ("ftp://ftp.php.net/incoming/outputfile", "w");
if (!$file) {
    echo "<p>Nie można otworzyć zdalnego pliku do zapisu.\n";
    exit;
}
/* Tutaj zapisujemy dane. */
fputs ($file, "$HTTP_USER_AGENT\n");
fclose ($file);
?>
]]>  
    </programlisting>
   </example>
  </para>
  <para>
   <note>
    <para>
     Być może powyższy przykład nasunął ci pomysł, by użyć tej metody
     do zdalnego zapisywania logów, ale, jak wspomniano wyżej, możesz
     tworzyć jedynie nowe pliki. Aby zrealizować zdalne logowanie powinieneś
     przyjrzeć się funkcji <function>syslog</function>.
    </para> 
   </note>
  </para>

 </chapter>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
